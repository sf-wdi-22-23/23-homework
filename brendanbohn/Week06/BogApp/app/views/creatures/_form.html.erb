 <%= simple_form_for @creature, html: { multipart: true} do |f|%>
		<div class="form-group">
			<%= f.input :name, input_html: { class: "form-control"}%>
		</div>
		<div class="form-group">
			<%= f.input :desc, input_html: { class: "form-control"}%>
		</div>
		<%= f.button :submit, class: "btn btn-primary" %>
<% end %>